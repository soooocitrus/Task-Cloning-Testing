Some previous experiment results:

`wc`: WordCount jobs in cycle

`wc_homogenous`: wc only on dic machines (no dicm/diccvm)

`multi_type`: initial experiments on 3 types of jobs

`trace`: first experiment try on trace